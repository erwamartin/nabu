<ul>
<% @users.each do |user| %>
    <li><%= link_to user.username, profile_path(user.username) %><%= user.email %><button class="btn-follow" data-id="<%= user.id %>" data-state=<% (@followings.include?(user.id)) ? ( %>"0"<% ) :( %>"1"<% ) %>><% (@followings.include?(user.id)) ? ( %>unfollow<% ) :( %>follow<% ) %></button></li>
<% end %>
</ul>