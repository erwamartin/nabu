<ul>
<% @users.each do |user| %>
    <li><%= link_to user.username, profile_path(user.username) %><%= user.email %><button class="btn-follow" data-id="<%= user.id %>" data-state=<% (@follows_current_user.include?(user.id)) ? ( %>"0"<% ) :( %>"1"<% ) %>><% (@follows_current_user.include?(user.id)) ? ( %>unfollow<% ) :( %>follow<% ) %></button></li>
<% end %>
</ul>